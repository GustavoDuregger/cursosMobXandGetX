name: curso
description: Gustavo Duregger RM79757

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  shimmer: ^1.1.1
  dio: ^3.0.9
  mobx: ^1.0.0
  flutter_mobx: ^1.0.0
  splashscreen: ^1.2.0
  url_launcher: ^5.2.0
  get: ^3.11.1
  cupertino_icons: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.7.2
  mobx_codegen: ^1.0.0 

flutter:

  uses-material-design: true

  assets:
    - assets/fiap.png
    - assets/gif.gif
